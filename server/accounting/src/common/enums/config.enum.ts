export const enum ConfigEnum {
    PORT = 'PORT',
    NODE_ENV = 'NODE_ENV',
    MONGO_URL = 'MONGO_URL',
    JWT = 'JWT',
    SWAGGER = 'SWAGGER',

    development = 'development',
    dev = 'dev'
}
