export const enum ConfigEnum {
    PORT = 'PORT',
    NODE_ENV = 'NODE_ENV',
    MONGO_URL = 'MONGO_URL',
    JWT = 'JWT',
    SWAGGER = 'SWAGGER',

    PRODUCTION = 'production',
    DEVELOPMENT = 'development',
    dev = 'dev'
}
